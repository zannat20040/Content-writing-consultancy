import React from 'react';

const Subheading = ({label}) => {
    return (
        <p className="font-inter text-gray-500 text-lg mt-5 text-center">
        {label}
      </p>
    );
};

export default Subheading;